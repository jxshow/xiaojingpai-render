# 鲸选AI 公众号设计系统

## 1. 气质与辨识度

面向 AI 工具实测、工作流与提示词分享。文章先可读，再有品牌；不把每段都做成卡片。
核心辨识点：字节绿的下半幅渐变标题/绿底章节签；AGI绿的 48px 栈式大编号 + 可选英文标签；杂志绿的刊头封面与导读卡；Pro蓝的数字徽章与蓝色居中标题。独立提示词卡跟随主题色。
只继承 WaytoAGI 的版式语言，不冒用其头像、署名、粉丝量或原创声明。
鲸选AI 品牌文案来自用户横幅：挖掘 PMF & TIP；每个人的 AI 顾问。

## 2. 色彩

机器唯一来源：assets/tokens.json。文章导出时解析为具体内联值，不携带 CSS 变量。

| 角色 | 字节绿 / AGI绿 / 杂志绿 | Pro蓝 |
| --- | --- | --- |
| 主色到渐变端点 | #2ea250 → #09fc3c | #0057ff → #17b7de |
| 正文 | #4e5969 | #252525 |
| 标题 | #1d2129 | 黑色 / 蓝色章节标题 |
| 文章背景 | #ffffff | #ffffff |
| 引用背景 | #f3fbf4 / #f5faf6 | #f1f8ff |
| 重点字 | 深绿到鲜绿的字色，无底线 | 加粗黑字为主，显式mark用蓝色 |
| 下划线关键词 | 黑粗字 + 2px 绿底线 | 黑粗字 + 2px 蓝色底线 |

字节绿网页预览原底色 #f8faf9 只用于预览环境；文章正文默认白底，避免整块灰底随复制进入微信。
荧光绿 #09fc3c 是用户指定品牌色，不换成原摸鱼绿 #059669。标题/刊头色带使用原生 linear-gradient，色块设置实色背景兜底。H1下半幅渐变被过滤时标题文字仍为黑色；纯色稳妥版使用实色边框。
重点字默认 steps：按完整字素分段插值着色，保留真正文字，无透明字；连续 CSS 裁剪渐变可显式 textGradient:continuous，必须提示过滤风险。另一份纯色稳妥输出将所有渐变改为实色，不改变文字。亮绿只用于短语，不整段染绿；亮色白底对比有限，不宣称满足所有无障碍对比标准。
提示词卡背景/虚线/标题来自所选主题。绿主题不残留紫色，Pro蓝采用浅蓝。
页脚黑 #101611，白 #ffffff；成功交互 #176b39；辅助文字 #667085；分隔 #e5e7eb。

## 3. 字体

中文正文系统栈：-apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', sans-serif。
代码栈：Consolas, 'SFMono-Regular', monospace。绝不改写提示词/代码中的空格和引号来模拟字体。

| 角色 | 字号 | 行高 | 字重 |
| --- | --- | --- | --- |
| H1 | 24px | 1.5 | 700 |
| H2（AGI绿编号） | 48px | 1 | 900 |
| H2（AGI绿英文标签） | 10px | 1.65 | 500 |
| H2 | 20px | 1.6（AGI绿 1.4） | 700（AGI绿 800） |
| H3 | 17px | 1.6 | 700 |
| 正文/引用/列表 | 16px | 1.85–1.9 | 400 |
| 提示词 | 14px | 1.9 | 400 |
| 代码 | 13px | 1.7 | 400 |
| 标签/图注/署名说明 | 12–14px | 1.65 | 400–600 |

AGI绿英文标签色 #a1a1aa，仅出现在编号与标题之间；原文未给出栏目英文名时整行省略，不自动补造。

## 4. 间距与布局

文章不固定宽高；预览阅读栏最大 720px；手机两侧各 16px。正文段间 12px；大节前 28px、后 12px；小节前 20px、后 8px；引用内边距 16px；卡片外距 20px；签名前距 32px。
文章根 padding:0；不输出虚构的 H1、空段、垫高容器、min-height。图片真实比例、display:block、height:auto、max-width:100%，媒体容器 line-height:0；图注单独恢复行高，段后默认 16px。
长单词/URL 使用 overflow-wrap:anywhere；不把普通中文正文逐字强制断行。H1 按原有逗号/冒号等标点分成 inline-block 短语，优先在语义边界折行，不改变原字词与标点；超长单段仍可自然换行。text-wrap:balance 仅辅助，不承担防断词的唯一机制。

## 5. 组件

render_article.mjs 中的组件以 tokens.json 驱动：

- title：字节绿下半幅渐变带；AGI绿/Pro蓝左对齐黑色；杂志绿使用独立封面，正文不再重复H1。
- heading：字节绿渐变绿签；杂志绿同一行“01 标题”，不加竖线、PART或英文副题；AGI绿使用栈式大编号——48px 主色编号单独一行，其下为可选的 10px 英文标签，再下为 20px 黑粗标题，章节前后留白加宽到 56/32px。绿主题H3为黑粗字加2px绿底线；正文重点字没有该底线。Pro蓝H2使用用户提供的1/2/3原图徽章（64px宽、自然比例、居中），下一行蓝色斜体标题；H3仍为蓝色斜体小标题。
- cover：杂志刊头标签/日期、黑色主标题、渐变重点副句、说明、品牌色带；18px圆角、24px内边距。不自动编造数字或标签。
- toc：146px宽的真实导读卡，inline-block 自然换行；桌面四列、窄屏两列，不靠横向隐藏区才能读全文。不伪装成可跳转按钮。
- summary：一句话导读框，正文重点渐变，不复用H3底线。
- paragraph/runs：正文、strong、em、mark、underline、link。重点默认少量，不自动每段强加。`underline` 给短关键词加主题色下划线（`components.underlineWidth` 2px + `underlineOffset` 2px），可与 strong 叠加；长句不要用下划线，用 strong。
- quote：一个块可含多个段落；空行留在同一外框，换行不新建两个引用。
- callout：提示、说明、适用条件卡；不得替代全部正文。
- prompt：主题浅色虚线外框，标题/正文分区；导出全文展开，预览可单独折叠为滚动区域。
- list：真实列表；table：只用于真实表格，长宽表优先转逐项说明，绝不作为布局工具。
- code：浅灰代码块，原样保留；不依赖外部高亮库。
- image：本地可预览，发布前必须替换为可用 HTTPS 图片地址。Pro蓝采用8px浅灰图框与独立图注，不预留作者栏的固定高度。缺图明示，不生成坏 src。
- signature：一篇一次鲸选AI 署名。可用提供的横幅，或轻量文字版；不得默认二者同时堆叠。

所有文章组件是真实 DOM，不把截图当可编辑文章。预览按钮有 hover/focus/disabled/status 状态。

## 6. 交互

微信文章片段无脚本、按钮、滚动限高或复制控件。预览外壳可选择“品牌渐变版/纯色稳妥版”，复制当前所见版本，另有只复制提示词及长卡滚动开关。复制目标始终来自干净的独立正文。
在用户点击时同步 copy 事件写入 text/html + text/plain；失败时尝试 Clipboard API；仍失败则选中正文并明确提示 Ctrl+C。失败不能显示成功。
预览无需动画、外部字体或网络库。键盘可到达全部按钮；复制状态用 aria-live 通知。

## 7. 层次

以轻底色和实边框为主；提示词卡/杂志一句话摘要可使用虚线；无投影堆叠。Pro蓝数字徽章是用户明确指定复用的JPG资产，不用CSS近似、不重绘、不裁切，不套正文图片的灰框；文章标题与正文仍是真文字。缺少原图的4及以上编号暂用普通蓝色数字并提示补图，不能重复1/2/3或仿造花边。字号与留白承担层级。

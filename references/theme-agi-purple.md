# AGI 紫的兼容入口

用户已要求统一为鲸选绿。`agi-purple` 只保留为旧输入的兼容 ID，渲染时映射到 `agi-green`，不再输出紫色主题。

请完整读取 [鲸选AGI绿](theme-agi-green.md)；新的输入与推荐预览使用 `agi-green`。旧 `assets/theme-previews/agi-purple/preview.html` 同步更新为绿色，方便已有书签继续使用。
